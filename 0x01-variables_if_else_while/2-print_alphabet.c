#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Determine if a random number is greater, equal or less than 5.
(*
 * Return: 0 on success
 */
int main(void)
{
	char c;

	/* your code goes there */
	for (c = 'a'; c <= 'z'; c++)
    {
        putchar("%c ", c);
    }
	return (0);

}
